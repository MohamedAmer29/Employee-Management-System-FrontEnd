// Shared session constants so the restore and the monitor stay in sync.
export const SESSION_WARNING_THRESHOLD_MS = 5 * 60 * 1000;
export const SESSION_CHECK_INTERVAL_MS = 30 * 1000;
export const SESSION_ACTIVITY_THROTTLE_MS = 20 * 1000;
